# services

Backend implementations live here.

The first runnable backend will be `api-hono` because version 1 targets Cloudflare Workers.

The other language folders are created now so the project shape supports later server deployments and multi-language backend study.
